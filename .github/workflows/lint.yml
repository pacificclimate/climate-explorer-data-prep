name: Lint

on: push

jobs:
  black:

    runs-on: ubuntu-22.04
    
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v4
      - uses: psf/black@stable
        with:
          black_args: ". --check"